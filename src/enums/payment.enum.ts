export enum PaymentMethod {
  CREDIT_CARD = 'credit_card',
  DEBIT_CARD = 'debit_card',
  PAYPAL = 'paypal',
}

export enum Status {
  COMPLETED = 'completed',
  PENDING = 'pending',
  FAILED = 'failed',
}
